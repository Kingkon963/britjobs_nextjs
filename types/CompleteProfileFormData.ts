type CompleteProfileFormData = {
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  gender: string;
  postcode: number;
  addressLine: string;
  city: string;
};

export default CompleteProfileFormData;
