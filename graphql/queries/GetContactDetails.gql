query GetContactDetails($userID: ID!) {
  contactDetails(filters: { user: { id: { eq: $userID } } }) {
    data {
      id
      attributes {
        firstName
        lastName
        aboutMe
        address {
          postcode
        }
      }
    }
  }
}
